name: Build and Deploy to Docker

on:
  push:
    branches:
      - main
      - dev

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}

      - name: Determine Dockerfile
        id: dockerfile
        run: |
          if [[ $GITHUB_REF == 'refs/heads/main' ]]; then
            echo "Using Dockerfile_Production"
            echo "DOCKERFILE=Dockerfile_Production" >> $GITHUB_ENV
            echo "PORT=5254" >> $GITHUB_ENV
            echo "DOCKER_NAME=cicd_production" >> $GITHUB_ENV
          else
            echo "Using Dockerfile_Development"
            echo "DOCKERFILE=Dockerfile_Development" >> $GITHUB_ENV
            echo "PORT=5253" >> $GITHUB_ENV
            echo "DOCKER_NAME=cicd_development" >> $GITHUB_ENV
          fi
      
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: khanganonymous/${{ env.DOCKER_NAME }}:latest
          file: ./${{ env.DOCKERFILE }}

      - name: Deploy on VPS
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_PASSWORD }}
          port: 22
          timeout: 300s
          script: |
            docker pull khanganonymous/${{ env.DOCKER_NAME }}:latest
            docker stop ${{ env.DOCKER_NAME }} || true
            docker rm ${{ env.DOCKER_NAME }} || true
            docker run -d -p ${{ env.PORT }}:${{ env.PORT }} --name ${{ env.DOCKER_NAME }} khanganonymous/${{ env.DOCKER_NAME }}:latest
